#!/bin/sh
if [ "${TAIXU_BUILD_GUARD_BYPASS:-0}" = "1" ] || [ "${TAIXU_BUILD_ENGINE:-0}" = "1" ]; then
    exec "${FLUTTER_HOME:-/opt/flutter}/bin/flutter" "$@"
fi
exec /bin/sh /opt/taixu/bin/taixu-build-guard flutter "$@"
